{
  "name": "contentplane-reference-audio-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "npm run typecheck && wrangler check startup && wrangler deploy --dry-run",
    "types": "wrangler types",
    "types:check": "wrangler types --check",
    "typecheck": "wrangler types && tsc --noEmit",
    "deploy:dry": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy --minify"
  },
  "devDependencies": {
    "typescript": "latest",
    "wrangler": "latest"
  }
}
